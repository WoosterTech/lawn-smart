[project]
name = "lawn-smart"
description = "A smart watering engine for lawn sprinkler systems."
readme = "README.md"
requires-python = ">=3.12,<4.0"
license-files = ["LICENSE"]
authors = [
  { name = "Karl Wooster", email = "karl@woostertech.com" },
]
# https://pypi.org/classifiers/
# Adjust as needed:
classifiers = [
  # Adjust as needed:
  "Development Status :: 4 - Beta",
  # "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  # Include this to avoid accidentally publishing to PyPI:
  "Private :: Do Not Upload",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
# ---- Main dependencies ----
dependencies = [
  "attrmagic>=0.2.3",
  "pydantic>=2.11.7",
  "pydantic-settings>=2.13.1",
  "rich>=14.0.0",
  "typer>=0.24.1",
]
dynamic = ["version"]

# ---- Dev dependencies ----

[project.urls]
Repository = "https://github.com/WoosterTech/lawn-smart"
# Homepage = "https://..."
# Documentation = "https://..."

[project.scripts]
# Add script entry points here:
lawn-smart = "lawn_smart.lawn_smart:cli"

# ---- Build system ----

# Dynamic versioning from:
# https://github.com/ninoseki/uv-dynamic-versioning/

# ---- Dev dependencies ----

[dependency-groups]
dev = [
  "basedpyright>=1.31.0",
  "codespell>=2.4.1",
  "deptry>=0.23.0",
  "ipython>=9.8.0",
  "jinja2>=3.1.6",
  "pydantic>=2.11.9",
  "pytest>=8.3.5",
  "pytest-sugar>=1.0.0",
  "rich>=14.0.0",
  "ruff>=0.11.9",
]

[build-system]
requires = ["hatchling", "uv-dynamic-versioning"]
build-backend = "hatchling.build"

[tool.codespell]
# Add here as needed:
# ignore-words-list = "foo,bar"
# skip = "foo.py,bar.py"

[tool.hatch.version]
source = "uv-dynamic-versioning"
# Note JSON schemas don't seem to be right for tool.hatch.version.source so
# this may cause false warnings in IDEs.
# https://github.com/ninoseki/uv-dynamic-versioning/issues/21

[tool.hatch.build.targets.wheel]
# The source location for the package.
packages = ["src/lawn_smart"]

# ---- Settings ----

[tool.pytest]
filterwarnings = []
norecursedirs = []
python_classes = ["Test*"]
python_files = ["*.py"]
python_functions = ["test_*"]
testpaths = [
  "src",
  "tests",
]

[tool.uv-dynamic-versioning]
vcs = "git"
style = "pep440"
bump = true
